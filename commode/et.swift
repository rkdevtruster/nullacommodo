import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(alignment: .bottomLeading) {
            Text("Hello, World!")
                .padding()
        }
    }
}
